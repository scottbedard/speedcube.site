/**
 * Pre-normalized vector coordinates.
 */
export type RawVectorObject = {
  x?: number,
  y?: number,
  z?: number,
};

/**
 * Vector coordinates.
 */
export type VectorObject = {
  x: number,
  y: number,
  z: number,
};